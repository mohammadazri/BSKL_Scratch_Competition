// Drizzle schema mirror of the Postgres tables defined in SCHEMA.md.
// Track 1 (track-1-db) owns this file — populates it with every table from SCHEMA.md
// and exports inferred types via $lib/types.ts.

export {};
